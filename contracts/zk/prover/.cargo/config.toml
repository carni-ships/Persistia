# Optimized for Apple Silicon (M3 Pro)
# Enables AArch64 crypto extensions for hardware-accelerated SHA-256 and AES
[target.aarch64-apple-darwin]
rustflags = [
    "-C", "target-cpu=native",
    "-C", "target-feature=+sha2,+aes,+neon",
]
