[package]
name = "token-standard"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
persistia-sdk = { path = "../persistia-sdk" }

[profile.release]
opt-level = "z"
lto = true
strip = true
